import CustomButton from "./CustomButton";
import ColorPicker from "./ColorPicker";
import AIPicker from "./AIPicker";
import FilePicker from "./FilePicker";
import Tabs from "./Tabs";



export { 
    CustomButton,
    AIPicker,
    ColorPicker,
    FilePicker,
    Tabs, 

};

// after importing, we can also export it within an object.
// now we can import (everything samelike doing export )custom button to homepage ../components
//first import here, then export and import from other places  as single components


